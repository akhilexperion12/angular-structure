import { Injectable } from '@angular/core';
@Injectable()
export class PermissionKeyService {

    public userPermission = {
        // Pros permission keys
        prosKey: [
        ],
        // Clients permission keys
        clientsKey: [
        ]
    };
}
